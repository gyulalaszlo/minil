(fn [a] (print a))

(def hello "Hello")
(def say-hello (fn [name] (console.log hello name)))

((fn []
(let name "Joe"
    (let sign "!"
        (print "Hey, " name sign)))
))


;; name "is" 13 or [1 3 "12"]
;; {a 12
;;  b 13}

;; (console.log "Hello" name)
;; (let [name "T"]
;;  (console.log "Hello" name))
;;
;; (let [a 0
;;      b 10]
;;      (console.log (+ a b)))
